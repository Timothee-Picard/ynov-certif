services:
  frontend:
    build:
      context: ./
      dockerfile: prod.Dockerfile
      target: prod
    restart: unless-stopped
    healthcheck:
      test: [ "CMD", "wget", "--spider", "http://frontend:3000/" ]
      interval: 30s
      timeout: 10s
      retries: 5