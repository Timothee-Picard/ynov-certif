{
  "name": "ynov-certif",
  "version": "1.1.0",
  "scripts": {
    "release": "standard-version",
    "release:push": "git push --follow-tags origin main"
  },
  "devDependencies": {
    "standard-version": "^9.5.0"
  }
}
